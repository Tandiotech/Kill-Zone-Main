# KILLZONE (Kill-Zone-Main) -> docker container on 127.0.0.1:5000
# Install:
#   sudo cp deploy/nginx/killzone.tandiotech.com.conf /etc/nginx/sites-available/killzone.tandiotech.com
#   sudo ln -s /etc/nginx/sites-available/killzone.tandiotech.com /etc/nginx/sites-enabled/
#   sudo nginx -t && sudo systemctl reload nginx
#   sudo certbot --nginx -d killzone.tandiotech.com     # adds the 443 block + redirect
server {
    listen 80;
    listen [::]:80;
    server_name killzone.tandiotech.com;

    # Let certbot rewrite this block into HTTPS after the cert is issued.
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket / HMR upgrade support
        proxy_set_header Upgrade    $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_read_timeout 90s;
    }
}
